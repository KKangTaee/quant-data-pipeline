interface:
  display_name: "Finance DB Pipeline"
  short_description: "Build finance ingestion, schema, loaders, and persistence flows."
  default_prompt: "Update the finance ingestion or DB pipeline with idempotent writes and clear source boundaries."
