// Compatibility export: the component directory/name remains stable for the
// Streamlit build, while the current renderer is the Decision Brief workspace.
export { DecisionBriefWorkspace as FinalReviewInvestmentReport } from "./DecisionBriefWorkspace"
export type {
  CandidateSelectorModel,
  DecisionBrief,
  DecisionWorkspaceIntent,
} from "./decisionBriefTypes"
