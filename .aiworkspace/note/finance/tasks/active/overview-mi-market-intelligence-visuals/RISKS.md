# Risks

- Altair can warn when chart inputs are empty. Empty states should still render a safe placeholder and show table empty-state copy.
- Heatmap values are current DB snapshot context, not trade recommendations.
