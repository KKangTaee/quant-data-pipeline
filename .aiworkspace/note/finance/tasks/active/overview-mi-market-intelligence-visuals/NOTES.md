# Notes

- Visuals are derived from the existing service DataFrames in `app/web/overview_dashboard.py`.
- No new loader, collector, or schema contract is introduced.
- Color scale uses red / near-white / teal around zero to avoid a one-note palette.
