# Notes

- The session date is based on `America/New_York`, not the local Korean calendar date.
- The banner uses a local rules-based NYSE calendar for common full-day holidays and common early closes. It does not attempt to predict one-off unscheduled exchange closures.
- Display includes KST equivalents because the local app is operated in Korea, while the exchange session itself remains ET-based.
