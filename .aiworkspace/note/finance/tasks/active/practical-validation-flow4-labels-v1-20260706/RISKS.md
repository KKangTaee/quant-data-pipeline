# Risks

- Existing Streamlit servers on ports 8501 and 8505 were launched from other or stale runtime states, so Browser QA used a fresh server on port 8511.
- Internal audit taxonomy names such as `Data Coverage Audit` remain in service / Final Review evidence contracts where they are module or packet section identifiers, not Flow 4 user-facing `보강 위치`.

