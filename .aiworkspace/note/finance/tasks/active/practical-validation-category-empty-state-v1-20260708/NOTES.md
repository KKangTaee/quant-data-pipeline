# Notes

- `applies=False` modules are already filtered before Flow 4 category group creation.
- The confusing `보강 항목 없음` state is created when a group has no `BLOCKED / NEEDS_INPUT / NOT_RUN` and no `PASS / READY`, typically a REVIEW-only group.
- Latest direction keeps REVIEW / final selection judgment out of Practical Validation visible actionable surfaces.
