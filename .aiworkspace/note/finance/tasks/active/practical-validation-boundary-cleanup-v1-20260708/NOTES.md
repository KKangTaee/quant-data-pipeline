# Notes

- `REVIEW` remains in the service model for downstream Final Review use, but Practical Validation no longer presents it as an actionable issue.
- The React component path name still says `fix_queue` for compatibility, but the visible UI is a validation conclusion summary.
- Flow 5 still owns save / Final Review move controls and may mention Final Review by design.
