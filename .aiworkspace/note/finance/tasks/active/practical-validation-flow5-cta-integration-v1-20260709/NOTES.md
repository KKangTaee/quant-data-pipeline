# Notes

- Flow5 currently owns save-only audit append and Final Review handoff button.
- Flow3 React component currently renders conclusion and next-step text only.
- Backtest Analysis already has a React intent-only handoff pattern via `backtest_handoff_action`; Practical Validation should reuse the same boundary pattern conceptually.
