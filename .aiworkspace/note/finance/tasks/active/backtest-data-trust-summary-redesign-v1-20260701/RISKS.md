# Risks

- The custom HTML/CSS panel is scoped by `data-trust-brief`, but future Streamlit theme changes may require visual tuning.
- Existing `use_container_width` deprecation warnings remain elsewhere in the app and are not part of this task.
