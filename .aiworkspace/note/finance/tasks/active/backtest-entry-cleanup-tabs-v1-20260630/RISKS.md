# Risks

- Browser QA confirmed Streamlit's `st.pills` markup still matches the scoped CSS selectors used by Overview.
- The retained research board service is now historical / auxiliary unless a future task explicitly gives it a new user-facing home.
