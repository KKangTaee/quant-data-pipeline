# Notes

- Backtest stage keys remain unchanged: `Backtest Analysis`, `Practical Validation`, `Final Review`.
- Only display labels changed to Korean-first labels. Route compatibility and legacy panel mapping remain owned by `app/web/backtest_workflow_routes.py` / `app/web/backtest_common.py`.
- The Backtest Analysis research placement service is not removed in this task; the current user-facing Backtest Analysis render path no longer calls the bottom board.
