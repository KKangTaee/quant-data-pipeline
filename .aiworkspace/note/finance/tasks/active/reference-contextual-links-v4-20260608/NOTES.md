# Notes

- Use `/guides` and `/glossary` as simple Reference entry points. Query deep-linking can wait until a later Reference polish task.
- Keep helper collapsed by default so dense work screens are not visually overwhelmed.
- The helper explains where to look; it does not decide validation / monitoring status.
