# Notes

- Current service summary is built in `build_overview_macro_context_cockpit`.
- Current UI renderer already separates cockpit from reading-flow sections, so 2차 can remain a narrow copy / density change.
- `summary.headline` now states the top mover as one sentence, while `summary.detail` states breadth / futures context and next reading order in two short sentences.
- Desktop Browser QA live data showed `WDC +15.6%`, `Technology` leadership, mixed futures/macro background, and six review checks.
