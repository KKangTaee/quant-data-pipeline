# Risks

- Browser QA may still require Streamlit startup with local DB availability.
- The UI can only summarize available snapshots; missing or stale local data should remain visible as context, not be hidden by smoother copy.
- No open implementation blocker after QA.
- Optional follow-up: if real use still feels dense, make lower-priority reference sections more aggressively collapsed rather than adding more cards.
