# Overview Market Context Macro Labels V15 Risks

## Risks

- No open implementation risk found in focused QA.
- Residual UX risk: if more Macro dimensions later become hard filters, the stage labels should be extended with explicit per-condition Korean names instead of falling back to generic `Macro 조건 적용`.
