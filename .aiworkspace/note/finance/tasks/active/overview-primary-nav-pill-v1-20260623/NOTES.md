# Overview Primary Nav Pill V1 Notes

## Decision

Use a custom HTML/CSS nav instead of trying to restyle Streamlit `segmented_control`. This avoids the current full-width four-way form-control look and gives Overview a clearer product navigation surface.

## Boundaries

- No tab membership change.
- No data read model change.
- No provider / schema / persistence change.
