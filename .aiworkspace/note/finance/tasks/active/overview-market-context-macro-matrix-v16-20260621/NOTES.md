# Overview Market Context Macro Matrix V16 Notes

## Notes

- The goal is visual coherence with the existing historical analog flow, not another copy explanation pass.
- The primary Macro read path should be compact enough to scan without reading provider/source detail.
- The old visible `ov-macro-sample-flow` and `ov-macro-delta-table` structures were removed from the primary path.
- Verbose condition source labels remain available in `Macro 조건 상세` when the compact audit is otherwise unavailable.
