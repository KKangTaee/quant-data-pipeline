# Notes

- Events tab still reads only `finance_meta.market_event_calendar` via the Overview service.
- Superseded rows remain hidden by the read model. Calendar UX focuses on active / stale current rows.
- The calendar view groups by stored `Date`; it is not a full calendar grid.
