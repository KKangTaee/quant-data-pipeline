# Overview Market Context Macro Clarity V14 Risks

- Macro conditioned sample can be very small. UI must not make the conditioned result look like a better prediction.
- Current macro backdrop preview buckets are simple thresholds. They should remain context/reference, not hard conditions, until separately validated.
- Matrix color strength uses median return magnitude, not positive-rate confidence. Users should read positive rate and worst path alongside the color.
