# Backtest Practical Validation Handoff Gate V1 Risks

## Known Risks

- Existing historical run records can still reflect older handoff behavior.
- This task does not redesign compare / weighted mix handoff gates.
- The enabled button still uses the existing Clean V2 source handoff path; this task does not create a new storage model.
