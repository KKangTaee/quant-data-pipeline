# Risks

- BLS may block direct automated HTML / ICS retrieval depending on network and request policy.
- Official release schedules can change during government shutdowns or agency updates; users should refresh before relying on dates.
- Macro calendar dates are event timing metadata, not the released economic values.
- In this local smoke, BLS returned HTTP 403. BEA GDP collection still succeeded and the combined job reported `partial_success`.
- CPI / PPI / Employment rows remain unavailable until BLS access succeeds or another official machine-readable path is added.
