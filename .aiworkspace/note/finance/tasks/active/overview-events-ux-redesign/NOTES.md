# Notes

- This is a UI/readability pass only. The event source of truth remains `finance_meta.market_event_calendar`.
- Source lane status is derived from the currently loaded rows and respects the selected Type filter: missing selected source rows show `Missing`, rows requiring validation/source action show `Review`, otherwise source blocks show `OK`.
- `Agenda` is the default interpretation layer for upcoming events; `Quality` is where estimate-only, not-confirmed, stale, or action-required rows are grouped.
- 2026-07-07 continuation: Events must remain a market background / investigation clue surface. It must not become a validation gate, buy/sell signal, monitoring signal, broker action, or automatic action surface.
- The React workbench should follow the Sentiment / Futures Macro custom component pattern: Python service owns DB reads and interpretation payloads; Python helper owns refresh/reload side effects; React owns display, local filter/toggle state, hover tooltip, and calendar/density interactions.
- The next service gap is not raw data availability. It is a React-ready structured payload for hero brief, grouped rails, trust review, calendar day buckets, density buckets, and lower evidence rows.
- 2026-07-07 2차: The Events calendar expansion now has a normalized taxonomy contract before collector expansion. `event_family` covers central_bank / macro / earnings / fixed_income / market_structure / corporate_action / other; `universe_scope` separates official macro, all-US market structure, S&P 500, Nasdaq-100, portfolio, watchlist, latest movers, and major-cap coverage; `source_authority` separates official / issuer-confirmed / provider-estimate / cross-checked / not-confirmed / conflict / unknown.
- Legacy rows remain readable. If taxonomy columns are missing, `build_market_events_snapshot()` derives the new display columns from `event_type`, `source_type`, `validation_status`, and `source`.
- 2차 intentionally did not add BLS/BEA/Census/ISM/Treasury/Cboe/S&P/Nasdaq/Russell collectors and did not scaffold React. Those are 3차~7차.
- 2026-07-07 3차: `collect_and_store_macro_calendar()` now accepts `include_census`, `include_ism`, and `include_treasury` in addition to BLS/BEA. Default Overview macro refresh now attempts the broader official calendar set, but source failures remain partial-success diagnostics rather than blocking other successful source rows.
- Data Health's Macro Calendar coverage gate still uses the existing core macro set to avoid turning the whole workspace stale before the new sources are populated. The expanded event families are available in Events rows and future workbench payloads first.
- 2026-07-07 4차: Earnings collection distinguishes the source list from the calendar authority. `symbol_source`/`universe_scope` describe which symbols were swept (`latest_movers`, `sp500`, `major_cap`, `nasdaq100`, `portfolio`, `watchlist`), while `source_authority` describes whether the date is provider estimate, Nasdaq cross-checked, or not confirmed.
- Portfolio and watchlist earnings are intentionally opened as injected symbol-loader boundaries, not guessed from unrelated saved portfolio registries. A future UI can pass explicit selected symbols into those loaders without making Events a validation gate or monitoring action.
- 2026-07-07 5차: Market-structure events are stored as calendar density/background context only. Holidays, early closes, options expiration, and Russell reconstitution are not validation gates, signals, or automated monitoring actions.
- The Cboe options expiration rows use the standard third-Friday calendar rule with exchange-holiday adjustment and keep the official Cboe calendar PDF URL as source evidence; if the PDF format becomes parseable later, the builder can be replaced without changing the row contract.
- 2026-07-07 6차: React should render `build_events_workbench_payload()` instead of inventing labels or source warnings. The payload owns the hero brief, boundary note, official-vs-estimate counts, rail grouping, trust sections, calendar buckets, density buckets, and raw evidence rows.
- Workbench rails are intentionally non-overlapping for display (`Recent major`, `Today`, `This Week`, `Next 30D`, `Later`), while hero counts keep user-facing cumulative counts for today / this week / next 30 days.
- 2026-07-07 7차: The first React scaffold is deliberately additive. It appears above the existing Streamlit details when the build is present, and does not remove Agenda / Calendar / Quality / Raw fallback yet.
- `events_workbench/node_modules/` is local build dependency output and must stay uncommitted. Commit only source files, package lock, and `component_static` build assets matching the existing component pattern.
