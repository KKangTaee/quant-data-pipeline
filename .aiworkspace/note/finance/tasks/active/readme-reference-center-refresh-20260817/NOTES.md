# Notes

## Decisions

- Keep internal destination keys `overview`, `institutional_portfolios`, and `ingestion`; they route current `Market Research`, `Institutional Holdings`, and `Data Operations`.
- Keep lowercase `overview` / `ingestion` search keywords where useful so old vocabulary still finds current help content.
- Remove README wording that makes `backtest-dev` port `8510` sound like a current shared fact; use it only as an example port.
