# Status

## 2026-06-16

- Started after the user approved implementing 1차~3차 together for `참고: 과거 유사 맥락` readability.
- Added a definition paragraph before the historical analog table so the user sees what similarity means before reading statistics.
- Added a compact summary strip and `먼저 읽을 결론` sentence before the detailed rows.
- Split the detailed table into `핵심 자산 요약` and `보조 자산 참고`, keeping the existing calculation and context-only boundary.
- Verification and Browser QA are recorded in `RUNS.md`.
