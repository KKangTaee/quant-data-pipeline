# Overview Market Movers Sector V4 Risks

- Sector breadth reflects currently returnable rows for the selected coverage / period; missing quotes can still skew sector participation until 5차 data quality UX clarifies trust state.
- Market-cap share uses current profile market cap from the existing read path when available. It is a size proxy, not portfolio weight guidance.
- Nasdaq coverage may still show `No Universe` depending on local DB state; 5차 owns the consistent coverage trust language.
- The heatmap uses Streamlit-rendered HTML/CSS; the fallback table remains available if a narrow viewport makes tiles harder to scan.
