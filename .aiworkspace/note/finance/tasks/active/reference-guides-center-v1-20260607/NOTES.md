# Notes

- Worktree is dirty at start with unrelated saved portfolio JSONL, `.DS_Store`, run history JSONL, and prior QA screenshots. These must not be reverted or staged.
- Reference is read-only. It can point users to owner screens but must not execute refresh / replay / persistence actions.
- Existing portfolio-selection guide should remain reachable because it contains useful Go / Review / Stop semantics.
- User-facing guide naming now uses `Operations > Portfolio Monitoring`; `Selected Dashboard` remains legacy implementation/file vocabulary only.
