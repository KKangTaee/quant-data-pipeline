# Notes

- `REVIEW` currently means too many things: PV caution, final decision input, monitoring follow-up, and future deployment readiness.
- `visible_in_practical_validation = bool(passed or remaining)` hides REVIEW-only applied categories.
- Flow4 page currently filters out REVIEW detail cards before rendering category detail cards.
- Final Review investability packet re-reads many PV audit groups, so copy must make it clear that PV evidence is consumed, not rerun.
