# Notes

- `event_status` values are `active`, `superseded`, and `stale`.
- Superseded logic is limited to `EARNINGS` rows with the same symbol and source.
- FOMC official rows are not affected by earnings lifecycle cleanup.
