# Notes

- selected approach is a deterministic two-release state machine
- raw quadrant remains an auditable candidate signal, not the official phase
- confirmation happens at the second release and is never backdated
- missing input resets the candidate streak
- all destination phases remain valid
- circular route UI is retained; four-quadrant UI remains out of scope
- asset checkpoint calculation and presentation are frozen
