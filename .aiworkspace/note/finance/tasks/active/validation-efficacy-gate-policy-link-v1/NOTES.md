# Notes

- This is a policy-link task, not a new evidence collection task.
- The previous audit intentionally left survivorship / universe as `REVIEW` unless explicit historical universe evidence exists.
- Gate policy should be strict enough to block `NEEDS_INPUT` and `BLOCKED`, but should leave `REVIEW` as review-required rather than pretending it is ready.
