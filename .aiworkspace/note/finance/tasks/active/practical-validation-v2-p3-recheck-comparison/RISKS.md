# Risks

- A comparison view can be mistaken as approval if execution boundary is not explicit.
- Missing Performance Recheck must remain `NEEDS_INPUT`, not pass.
- UI verification in a live Streamlit browser session was not run; compile, service contracts, and boundary checks passed.
