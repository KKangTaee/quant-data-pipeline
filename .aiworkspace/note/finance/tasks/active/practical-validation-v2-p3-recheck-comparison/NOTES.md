# Notes

- User explicitly prefers validation data collection / DB persistence when needed for verification, but not memo-like or preset-like storage.
- This task should only read current selected decision and session-state Performance Recheck result.
- Thresholds intentionally match the existing Review Signals board: CAGR delta breach below `-0.03`, MDD delta breach below `-0.05`, benchmark spread breach below `0.00`.
- Period coverage is treated as `WATCH` when the recheck does not extend beyond the selected baseline end date.
