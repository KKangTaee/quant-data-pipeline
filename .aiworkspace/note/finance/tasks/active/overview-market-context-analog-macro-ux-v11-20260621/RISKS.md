# Risks

- Streamlit widgets cannot be embedded inside raw HTML from `st.markdown`, so the controls are placed immediately before the HTML analog analysis section and visually labeled as part of that flow.
- Full PIT sector membership remains outside this V11 UX task.
- Macro comparison can become overconfident if wording implies forecast or trading signal; copy must remain historical-reference only.
- The Macro dimension audit can still feel text-heavy with many stored macro/event/sentiment rows. This task changed structure first; collapsing or summarizing the lowest-priority details is a possible follow-up.
