# Notes

- Existing dirty/generated files are present before this task: run history JSONL, registry JSONL, and QA PNGs. They are not part of this task and should not be staged.
- Gate behavior is intentionally strict: missing net performance policy remains a selected-route blocker through Backtest Realism policy evidence.
- `_apply_real_money_hardening` was already the correct policy sink. The fix is upstream propagation: GTAA / GRS / Risk Parity / Dual Momentum now receive strict-compatible promotion policy defaults and include them in Backtest Analysis meta/source contract before Practical Validation.
- `Backtest Realism Audit > Net performance policy` changed from missing-threshold `REVIEW` to `PASS` for the fresh GRS run because both `net_cagr_spread` and `promotion_min_net_cagr_spread` are present in the source-derived evidence.
- Equal Weight was not given hidden proof. If net cost curve / turnover proof is missing, selected-route preflight still blocks through the Backtest Realism policy group.
