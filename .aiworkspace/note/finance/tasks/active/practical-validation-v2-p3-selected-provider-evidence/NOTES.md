# Notes

- Existing provider context service: `app/services/backtest_practical_validation_provider_context.py`.
- Existing Practical Validation UI already renders look-through board from provider context.
- Selected Dashboard should reuse compact provider context instead of storing full holdings / raw provider rows.
- Candidate replay contract can provide ticker list. For provider evidence, benchmark tickers should be excluded from portfolio symbol weights.
