# Risks

- The new result header uses scoped custom HTML/CSS. Future Streamlit theme changes may require visual tuning.
- Practical Validation handoff now appears after the detail tabs, so users must scroll lower to send a candidate forward. This is intentional because the handoff is a next-step action, not the primary result.
