# Notes

- 3차는 새 데이터 모델이나 provider 없이 현재 selected mode read model을 재구성하는 UI 단계다.
- Chart workspace facts는 ranking mode에 따라 return, volume, relative volume 중 기존 row에 있는 값을 사용한다.
- 기존 상세 표와 Why It Moved / 선택 종목 조사 흐름은 5차에서 별도로 다듬는다.
