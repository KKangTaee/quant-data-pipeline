# Overview Market Context Macro Intersection V18 Risks

## Risks

- Resolved in this slice: futures count is shown with `계산 가능 N회` when not all broad anchors have computable futures buckets.
- Resolved in this slice: the final cell says `두 조건 모두`, so users do not have to infer an order-dependent sequence.
- Residual: sample can still become very small after the intersection; keep `pilot 표본 좁음` and broad comparison visible.
