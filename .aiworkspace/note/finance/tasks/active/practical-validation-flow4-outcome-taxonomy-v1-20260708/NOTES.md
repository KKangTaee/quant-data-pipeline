# Notes

- `REVIEW`는 제거하지 않는다. 자동 차단이 아니라 Final Review에서 판단할 근거로 남긴다.
- `READY`는 사용자-facing label에서는 `PASS`와 동일하게 통과로 읽는다.
- `BLOCKED`는 현재 상태로 실전 사용이 어렵다는 결론이지만, 영구 폐기 의미가 아니라 후보 수정 또는 source 재구성이 필요한 상태다.
