# Overview Market Context Tab V1 Risks

## Residual Risks

- Streamlit tabs do not preserve a custom default after state changes beyond natural first-tab ordering; first-tab placement is the intended default.
- Moving the refresh button into `Market Context` makes it less visible while another deep tab is selected, which is intentional for cleaner deep-tab analysis.
