# Overview Market Context Tab V1 Notes

## Decisions

- `Market Context` should be the first Overview tab before `Market Movers`.
- `Deep Tab 읽는 순서` and `Overview Map` move with the cockpit because they explain where to go after reading the summary.
- Overview session banner can remain above the tabs as page-level context.
