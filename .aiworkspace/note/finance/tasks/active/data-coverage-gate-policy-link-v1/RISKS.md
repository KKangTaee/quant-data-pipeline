# Risks

- Older validation rows without `data_coverage_audit` may become selected-route blocked or review-required until revalidated.
- This task does not solve survivorship source collection; it prevents weak data coverage from being treated as ready.
