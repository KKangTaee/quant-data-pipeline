# Risks

- The disabled `유니버스 기준` control is still a native Streamlit button. It fixes slot consistency but is not a fully custom control system.
- Existing generated/local artifacts in the worktree must remain unstaged.
