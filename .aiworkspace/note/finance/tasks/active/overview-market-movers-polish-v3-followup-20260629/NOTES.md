# Notes

- Top1000 / Top2000 daily refresh has no separate universe refresh action in this surface. The UI now keeps the slot stable with a disabled `유니버스 기준` button and a tooltip instead of an inline caption.
- Sector breadth service model may still expose `leaders` for compatibility, but the main map no longer renders the duplicate leader strip.
