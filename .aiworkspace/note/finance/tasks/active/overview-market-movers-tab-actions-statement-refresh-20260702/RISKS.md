# Risks

- EDGAR statement refresh is heavier than metadata lookup; keep the Overview action scoped to the selected symbol.
- Streamlit button clicks rerun scripts; use a fragment and same-place result rendering to reduce scroll jumps.
