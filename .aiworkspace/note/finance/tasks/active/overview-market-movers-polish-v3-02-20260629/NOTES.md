# Notes

## 2026-06-29

- The old command strip and data trust strip are still available for compatibility and older source-contract tests.
- The actual `render_market_movers_snapshot` path now uses the unified summary, so the top-of-page story is single-pass.
- Coverage trust details remain intentionally secondary because raw diagnostics should not be the main Market Movers UX.
