# Notes

- Owning module is `app/web/final_selected_portfolio_dashboard.py`.
- Runtime contract appears unaffected by section order changes because portfolio setup persistence and scenario result session-state keys are independent from render position.
- The existing top-level flow docs still describe the old shelf/setup-first order and need sync after implementation.
