interface:
  display_name: "Finance DB Pipeline"
  short_description: "Build finance ingestion and DB persistence."
  default_prompt: "Use $finance-db-pipeline to update finance ingestion or DB persistence with clear source boundaries."
