interface:
  display_name: "Finance Backtest Web Workflow"
  short_description: "Build and debug the Backtest Streamlit flow."
  default_prompt: "Use $finance-backtest-web-workflow to update the Backtest UI while preserving route and registry boundaries."
