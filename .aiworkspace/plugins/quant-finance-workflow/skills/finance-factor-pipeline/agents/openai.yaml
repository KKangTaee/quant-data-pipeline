interface:
  display_name: "Finance Factor Pipeline"
  short_description: "Build finance factor workflows safely."
  default_prompt: "Use $finance-factor-pipeline to update a finance factor workflow while preserving timing and storage semantics."
